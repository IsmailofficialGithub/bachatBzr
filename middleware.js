// middleware.ts

export async function middleware(req) {
 
  console.log('Authenticated user:')

}

export const config = {
  // matcher: ['/dashboard/:path*', '/profile/:path*','/admin/:path*'], // customize this
}
